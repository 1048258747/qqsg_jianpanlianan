# QQ三国键盘连按工具 - 开发提示词

## 项目概述
创建一个Windows平台的键盘连按工具，用于游戏《QQ三国》的自动按键辅助。支持键盘连按和流程编辑器两种模式。

## 技术栈
- **语言**: Python 3.7+
- **GUI框架**: Tkinter (Python内置)
- **系统依赖**: ctypes (Windows API调用)
- **系统托盘**: pystray + pillow
- **图像识别**: opencv-python + pyautogui + numpy

## 核心功能需求

### 1. 窗口管理
- 使用Windows API (EnumWindows) 枚举所有可见窗口
- 使用正则表达式匹配窗口标题（如 "QQ三国.*线"）
- 支持多窗口选择
- 锁定目标窗口句柄(hwnd)
- 流程执行时自动切换到目标窗口（SetForegroundWindow）

### 2. 键盘模拟
- 使用PostMessage发送WM_KEYDOWN/WM_KEYUP消息
- 构建虚拟键码映射表(VK_CODES)
- 支持字母(A-Z)、数字(0-9)、功能键(F1-F12)、方向键等
- 可配置按键间隔(毫秒)和按键时长(毫秒)
- 按键序列循环执行（如 A,B,C 按顺序循环）
- **随机抖动功能**: 在预设值附近随机波动，模拟人工操作

### 3. 系统托盘
- 使用pystray创建托盘图标
- 隐藏窗口到托盘（root.withdraw()）
- 托盘菜单：显示窗口、退出程序
- 点击托盘图标恢复窗口（root.deiconify()）
- 关闭窗口时提示选择退出或隐藏到托盘

### 4. 图像识别与流程引擎
- **图像识别**: OpenCV模板匹配，支持中文路径
- **鼠标控制**: pyautogui 实现鼠标点击和移动
- **流程引擎**: 支持步骤序列执行、条件判断、循环控制
- **流程循环**: 支持固定次数循环和无限循环

### 5. 用户界面
- **键盘连按页面**: 窗口管理、连按设置、控制按钮（紧凑布局）
- **流程编辑器页面**: 流程管理、步骤添加、步骤列表、流程控制
- 调试日志区域：显示运行状态

## 项目结构
```
项目目录/
├── main.py              # 主程序（所有代码已合并到单个文件）
├── requirements.txt     # 依赖清单
├── README.md           # 使用说明
└── 开发提示词.md       # 本文件
```

## 依赖包
```
pystray>=0.19.4
pillow>=10.0.0
opencv-python>=4.8.0
pyautogui>=0.9.54
numpy>=1.24.0
```

## 关键代码实现要点

### 1. 窗口枚举（main.py）
```python
import ctypes
from ctypes import wintypes

user32 = ctypes.windll.user32

def get_all_windows():
    results = []
    def callback(hwnd, lParam):
        if user32.IsWindowVisible(hwnd):
            length = user32.GetWindowTextLengthW(hwnd)
            if length > 0:
                buffer = ctypes.create_unicode_buffer(length + 1)
                user32.GetWindowTextW(hwnd, buffer, length + 1)
                title = buffer.value
                if title:
                    results.append((hwnd, title))
        return True
    WNDENUMPROC = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
    user32.EnumWindows(WNDENUMPROC(callback), 0)
    return results
```

### 2. 按键发送（main.py）
```python
import random
import time

WM_KEYDOWN = 0x0100
WM_KEYUP = 0x0101

def key_press(hwnd, key, duration):
    vk_code = get_vk_code(key)
    scan_code = user32.MapVirtualKeyW(vk_code, 0)
    lParam_down = scan_code << 16
    lParam_up = (scan_code << 16) | (1 << 30) | (1 << 31)
    user32.PostMessageW(hwnd, WM_KEYDOWN, vk_code, lParam_down)
    time.sleep(duration)
    user32.PostMessageW(hwnd, WM_KEYUP, vk_code, lParam_up)

# 带随机抖动的按键发送
def key_press_with_jitter(hwnd, key, base_duration, jitter):
    duration_with_jitter = base_duration + random.randint(-jitter, jitter)
    duration_with_jitter = max(10, duration_with_jitter)
    key_press(hwnd, key, duration_with_jitter / 1000.0)
```

### 3. 系统托盘（main.py）
```python
import pystray
from PIL import Image, ImageDraw
import threading

def _setup_tray(self):
    def run_tray():
        menu = pystray.Menu(
            pystray.MenuItem("显示窗口", self._show_window, default=True),
            pystray.MenuItem("退出程序", self._exit_from_tray)
        )
        self.tray_icon = pystray.Icon(
            "QQ三国连按工具",
            self._create_tray_image(),
            "QQ三国键盘连按工具",
            menu
        )
        self.tray_icon.run()
    
    self.tray_thread = threading.Thread(target=run_tray, daemon=True)
    self.tray_thread.start()

def _on_close(self):
    result = messagebox.askyesnocancel("退出确认", "是否完全退出程序？\n\n选择'否'则隐藏到系统托盘")
    if result is None:
        return
    elif result:
        self._do_exit()
    else:
        self._hide_to_tray()
```

### 4. 随机抖动实现（main.py）
```python
class AutoKeyPresser:
    def __init__(self):
        self.jitter = 50  # 随机抖动范围(毫秒)
    
    def _key_loop(self):
        while self.running:
            # 按键时长带抖动
            duration_with_jitter = self.press_duration + random.randint(-self.jitter, self.jitter)
            duration_with_jitter = max(10, duration_with_jitter)
            key_press(hwnd, current_key, duration_with_jitter / 1000.0)
            
            # 按键间隔带抖动
            interval_with_jitter = self.interval + random.randint(-self.jitter, self.jitter)
            interval_with_jitter = max(10, interval_with_jitter)
            time.sleep(interval_with_jitter / 1000.0)
```

### 5. 图像识别（image_automation.py）
```python
import cv2
import numpy as np

class ImageRecognizer:
    def find_image_on_screen(self, template_path, confidence=0.8):
        # 支持中文路径
        template_data = np.fromfile(template_path, dtype=np.uint8)
        template = cv2.imdecode(template_data, cv2.IMREAD_COLOR)
        
        # 模板匹配
        result = cv2.matchTemplate(screen_gray, template_gray, cv2.TM_CCOEFF_NORMED)
        min_val, max_val, min_loc, max_loc = cv2.minMaxLoc(result)
        
        if max_val >= confidence:
            return True, max_loc[0], max_loc[1], max_val, None
        return False, 0, 0, max_val, "未找到图像"
```

### 6. 流程引擎（flow_engine.py）
```python
class FlowEngine:
    def start(self, loop_count=1):
        """启动流程执行
        Args:
            loop_count: 流程循环次数（1表示执行一次，0表示无限循环）
        """
        self.loop_count = loop_count
        # 激活目标窗口
        if self.target_hwnd:
            windll.user32.SetForegroundWindow(self.target_hwnd)
        
        thread = threading.Thread(target=self._execute_flow, daemon=True)
        thread.start()
    
    def _execute_flow(self):
        if self.loop_count == 0:
            # 无限循环
            while self.running:
                self._execute_steps(self.steps, 0, len(self.steps))
        else:
            # 固定次数循环
            for i in range(self.loop_count):
                self._execute_steps(self.steps, 0, len(self.steps))
```

## 使用说明
1. 安装依赖: `pip install -r requirements.txt`
2. 运行程序: `python main.py`
3. 查找并锁定游戏窗口
4. 设置按键序列、间隔、时长和抖动范围
5. 开始连按
6. 关闭窗口时可选择隐藏到托盘继续运行

## 流程编辑器使用
1. 切换到"流程编辑器"选项卡
2. 点击"新建流程"或"打开流程"
3. 添加步骤（图像点击、图像判断、键盘按键等）
4. 设置流程循环次数（底部控制区域）
5. 点击"执行流程"

## 注意事项
- 某些游戏需要管理员权限运行才能接收按键
- 建议关闭中文输入法或使用自动切换功能
- 随机抖动功能可让按键更自然，避免被检测
- 抖动值设为0则完全精确，无随机性
- 确保游戏窗口在前台
- 图像识别需要模板图像文件（支持png、jpg等格式）
- 流程循环次数设为0表示无限循环，需手动停止
